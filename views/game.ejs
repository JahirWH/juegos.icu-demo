<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= game.title %> - Juegos.icu</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #fff;
            min-height: 100vh;
            font-family: 'Arial', sans-serif;
            line-height: 1.5;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 25px;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
        }

        .logo {
            font-size: 2rem;
            color: #fff;
            font-weight: bold;
        }

        .logo a {
            text-decoration: none;
            color: white;
        }

        .inicio {
            font-weight: bold;
            text-decoration: none;
            font-size: 1.1rem;
            color: white;
            background: #4a90e2;
            border-radius: 6px;
            padding: 8px 16px;
            transition: background 0.3s ease;
        }

        .inicio:hover {
            background: #2d6da3;
        }

        .main-content {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px 15px;
        }

        .game-section {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        .game-header {
            margin-bottom: 20px;
        }

        .game-title {
            font-size: 2.2rem;
            color: #fff;
            margin-bottom: 12px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .game-tags {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .game-category {
            background: #4a90e2;
            padding: 8px 16px;
            border-radius: 18px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .game-type {
            background: #e24a4a;
            padding: 8px 16px;
            border-radius: 18px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .game-container {
            display: grid;
            grid-template-columns: 200px 1fr 200px;
            gap: 20px;
            align-items: start;
            margin: 20px 0;
        }

        .ad-sidebar {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 8px;
            padding: 15px;
            min-height: 600px;
            display: flex;
            flex-direction: column;
            gap: 15px;
            position: sticky;
            top: 20px;
        }

        .ad-slot {
            background: linear-gradient(45deg, #4a90e2, #2d6da3);
            border-radius: 6px;
            padding: 15px;
            text-align: center;
            color: white;
            font-size: 0.9rem;
            min-height: 150px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            transition: transform 0.3s ease;
            cursor: pointer;
        }

        .ad-slot:hover {
            transform: scale(1.02);
        }

        .ad-slot h3 {
            font-size: 1rem;
            margin-bottom: 8px;
        }

        .ad-slot p {
            font-size: 0.8rem;
            opacity: 0.9;
        }

        .game-frame {
            width: 100%;
            height: 600px;
            background: #000;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
        }

        .game-frame iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .game-description {
            background: rgba(255, 255, 255, 0.08);
            padding: 18px;
            border-radius: 8px;
            color: #e8e8e8;
            margin-top: 15px;
            border-left: 4px solid #4a90e2;
        }

        .game-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 15px;
            font-size: 0.9rem;
            color: #bdc3c7;
            flex-wrap: wrap;
            gap: 10px;
        }

        .game-author {
            font-style: italic;
            color: #888;
        }

        .game-credits a {
            color: #4a90e2;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .game-credits a:hover {
            color: #2d6da3;
        }

        .similar-games {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        .similar-games h2 {
            color: #fff;
            font-size: 1.6rem;
            margin-bottom: 20px;
            text-align: center;
        }

        .games-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 18px;
            list-style: none;
        }

        .game-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .game-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        .game-card a {
            text-decoration: none;
            color: #fff;
            display: block;
            padding: 12px;
            text-align: center;
        }

        .game-card img {
            width: 100%;
            height: 130px;
            object-fit: cover;
            border-radius: 6px;
            margin-bottom: 8px;
        }

        .game-card span {
            font-size: 0.9rem;
            color: #fff;
            font-weight: 500;
        }

        .no-games {
            text-align: center;
            color: #ccc;
            font-style: italic;
            padding: 20px;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background: rgba(0, 0, 0, 0.8);
            color: rgba(255, 255, 255, 0.7);
            margin-top: 30px;
        }

        /* Mobile Responsive */
        @media (max-width: 1024px) {
            .game-container {
                grid-template-columns: 1fr;
                gap: 15px;
            }
            
            .ad-sidebar {
                display: none;
            }
            
            .main-content {
                padding: 15px 10px;
            }
        }

        @media (max-width: 768px) {
            .navbar {
                padding: 12px 15px;
            }
            
            .logo {
                font-size: 1.6rem;
            }
            
            .inicio {
                font-size: 1rem;
                padding: 6px 12px;
            }
            
            .game-section, .similar-games {
                padding: 18px;
            }
            
            .game-title {
                font-size: 1.8rem;
            }
            
            .game-frame {
                height: 400px;
            }
            
            .games-grid {
                grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
                gap: 12px;
            }
        }

        @media (max-width: 480px) {
            .navbar {
                flex-direction: column;
                gap: 10px;
                text-align: center;
            }
            
            .game-frame {
                height: 300px;
            }
            
            .game-info {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }
        }

        /* Ad animations */
        .ad-slot:nth-child(1) { animation: pulse 4s infinite; }
        .ad-slot:nth-child(2) { animation: pulse 4s infinite 1s; }
        .ad-slot:nth-child(3) { animation: pulse 4s infinite 2s; }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">
            <a href="/">🎮 Juegos.icu</a>
        </div>
        <a class="inicio" href="/">Inicio</a>
    </nav>

    <div class="main-content">
        <section class="game-section">
            <div class="game-header">
                <h1 class="game-title"><%= game.title %></h1>
                <div class="game-tags">
                    <div class="game-category"><%= game.category %></div>
                    <% if (game.type === 'popular') { %>
                        <div class="game-type">Popular</div>
                    <% } else if (game.type === 'new') { %>
                        <div class="game-type">Nuevo</div>
                    <% } %>
                </div>
            </div>

            <div class="game-container">
                <!-- Publicidad Izquierda -->
                <div class="ad-sidebar">
                    <div class="ad-slot">
                        <h3>Publicidad</h3>
                        <p>Espacio publicitario disponible</p>
                    </div>
                    <div class="ad-slot">
                        <h3>Publicidad</h3>
                        <p>Tu anuncio aquí</p>
                    </div>
                    <div class="ad-slot">
                        <h3>Publicidad</h3>
                        <p>Publicidad </p>
                    </div>
                </div>

                <!-- Frame del Juego -->
                <div class="game-frame">
                    <%- game.content %>
                </div>

                <!-- Publicidad Derecha -->
                <div class="ad-sidebar">
                    <div class="ad-slot">
                        <h3> Juegos</h3>
                        <p>Descubre más juegos increíbles</p>
                    </div>
                    <div class="ad-slot">
                        <h3> Publicidad</h3>
                        <p>Promociones especiales</p>
                    </div>
                    <div class="ad-slot">
                        <h3> Publicidad</h3>
                        <p>Contenido exclusivo</p>
                    </div>
                </div>
            </div>

            <% if (game.description) { %>
                <div class="game-description">
                    <%= game.description %>
                </div>
            <% } %>

            <% if (game.author || game.credit_url) { %>
                <div class="game-info">
                    <% if (game.author) { %>
                        <div class="game-author">Autor: <%= game.author %></div>
                    <% } %>
                    <% if (game.credit_url) { %>
                        <div class="game-credits">
                            <a href="<%= game.credit_url %>" target="_blank">Créditos</a>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </section>

        <section class="similar-games">
            <h2><%= isIndependent ? 'Otros juegos independientes en' : 'Juegos similares en' %> <%= game.category %></h2>
            <% if (similarGames && similarGames.length > 0) { %>
                <ul class="games-grid">
                    <% similarGames.forEach(function(similarGame) { %>
                        <li class="game-card">
                            <a href="/game/<%= similarGame.id %>">
                                <img src="<%= similarGame.icon %>" alt="<%= similarGame.title %>">
                                <span><%= similarGame.title %></span>
                            </a>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <div class="no-games">No hay juegos similares disponibles.</div>
            <% } %>
        </section>
    </div>

    <footer class="footer">
        <p>&copy; 2025 Juegos.icu - Todos los derechos reservados.</p>
    </footer>

    <script>
        console.log('Juego cargado:', <%= JSON.stringify(game) %>);
        
        // Simulación de clicks en publicidad
        document.querySelectorAll('.ad-slot').forEach(ad => {
            ad.addEventListener('click', function() {
                console.log('Click en publicidad:', this.textContent);
                // Aquí puedes agregar tracking de analytics
            });
        });
    </script>
</body>
</html>