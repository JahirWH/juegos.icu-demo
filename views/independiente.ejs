<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= game.title %> - Juegos Independientes</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            color: #fff;
            min-height: 100vh;
            font-family: 'Arial', sans-serif;
            line-height: 1.5;
        }

        .header {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px 20px;
            text-align: center;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            display: flex;
        }

        .logo {
            font-size: 1.8rem;
            color: #fff;
            font-weight: bold;
            text-decoration: none;
        }
        .text{
            margin: auto;
            margin-right: 10px;
        }

        .main-content {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px 15px;
        }

        .game-section {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .game-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .game-title {
            font-size: 2rem;
            margin-bottom: 10px;
            color: #fff;
            font-weight: bold;
        }

        .game-tags {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
        }

        .tag {
            background: #3498db;
            padding: 6px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
            color: #fff;
        }

        .tag.type {
            background: #e74c3c;
        }

        .game-frame {
            width: 100%;
            height: 500px;
            background: #000;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            margin-bottom: 15px;
        }

        .game-frame iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        .game-description {
            background: rgba(255, 255, 255, 0.08);
            padding: 15px;
            border-radius: 8px;
            color: #e8e8e8;
            font-size: 1rem;
            border-left: 4px solid #3498db;
        }

        .game-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 10px;
            font-size: 0.9rem;
            color: #bdc3c7;
            flex-wrap: wrap;
            gap: 10px;
        }

        .game-author {
            font-style: italic;
        }

        .game-credits a {
            color: #3498db;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .game-credits a:hover {
            color: #2980b9;
        }

        .similar-games {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .similar-games h2 {
            color: #fff;
            font-size: 1.5rem;
            margin-bottom: 15px;
            text-align: center;
            font-weight: 600;
        }

        .games-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
            gap: 15px;
            list-style: none;
        }

        .game-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            overflow: hidden;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .game-card:hover {
            transform: translateY(-3px);
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
        }

        .game-card a {
            text-decoration: none;
            color: #fff;
            display: block;
            padding: 12px;
            text-align: center;
        }

        .game-card img {
            width: 100%;
            height: 120px;
            object-fit: cover;
            border-radius: 6px;
            margin-bottom: 8px;
        }

        .game-card span {
            font-size: 0.9rem;
            color: #fff;
            font-weight: 500;
            display: block;
            line-height: 1.3;
        }

        .no-games {
            text-align: center;
            color: #bdc3c7;
            font-style: italic;
            padding: 20px;
        }

        .footer {
            text-align: center;
            padding: 15px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            margin-top: 20px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main-content {
                padding: 15px 10px;
            }
            
            .game-section, .similar-games {
                padding: 15px;
                margin-bottom: 15px;
            }
            
            .game-title {
                font-size: 1.6rem;
            }
            
            .game-frame {
                height: 350px;
            }
            
            .games-grid {
                grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
                gap: 12px;
            }
            
            .game-card img {
                height: 100px;
            }
            
            .game-info {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }
        }

        @media (max-width: 480px) {
            .header {
                padding: 12px 15px;
            }
            
            .logo {
                font-size: 1.5rem;
            }
            
            .main-content {
                padding: 10px;
            }
            
            .game-frame {
                height: 280px;
            }
            
            .games-grid {
                grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
                gap: 10px;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <a href="/" class="logo">🎮 Juegos.icu</a>
        <a href="/" class="logo text" >  Inicio</a>
    </header>

    <div class="main-content">
        <section class="game-section">
            <div class="game-header">
                <h1 class="game-title"><%= game.title %></h1>
                <div class="game-tags">
                    <span class="tag"><%= game.category %></span>
                    <% if (game.type === 'popular') { %>
                        <span class="tag type">Popular</span>
                    <% } else if (game.type === 'new') { %>
                        <span class="tag type">Nuevo</span>
                    <% } %>
                </div>
            </div>

            <div class="game-frame">
                <%- game.content %>
            </div>

            <% if (game.description) { %>
                <div class="game-description">
                    <%= game.description %>
                </div>
            <% } %>

            <% if (game.author || game.credit_url) { %>
                <div class="game-info">
                    <% if (game.author) { %>
                        <div class="game-author">Autor: <%= game.author %></div>
                    <% } %>
                    <% if (game.credit_url) { %>
                        <div class="game-credits">
                            <a href="<%= game.credit_url %>" target="_blank">Ver créditos</a>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </section>

        <section class="similar-games">
            <h2>Más juegos de <%= game.category %></h2>
            <% if (similarGames && similarGames.length > 0) { %>
                <ul class="games-grid">
                    <% similarGames.forEach(function(similarGame) { %>
                        <li class="game-card">
                            <a href="/independiente/<%= similarGame.id %>">
                                <img src="<%= similarGame.icon %>" alt="<%= similarGame.title %>">
                                <span><%= similarGame.title %></span>
                            </a>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <div class="no-games">No hay juegos similares disponibles</div>
            <% } %>
        </section>
    </div>

    <footer class="footer">
        <p>&copy; 2025 Juegos.icu. Todos los derechos reservados.</p>
    </footer>
</body>
</html>